/// <reference types="cypress" />
/*///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
//	TESTS
//
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */


describe('Create {{ModelClass}} model in backend', () => {



/*///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
//	CREATE {{uppercase}}
//
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */


	it('create with correct data', () => {

		const random = Math.random().toString(36).substring(7);

		// arrange: got to edit {{slug}}
		cy.backendLogin();
		cy.visit('/backend/{{slug}}/edit');
		cy.wait(2000);

		// act
		cy.get('#input-name').type('Cypress Test-{{ModelClass}} ' + random);
		cy.get('#input-published-start').click();
		cy.wait(1000); // popup opening
		cy.get('.date-calendar-day.today').first().click();
		cy.get('.date-selector-confirm').click();
		cy.wait(1000); // popup closing
		cy.get('#input-public-0').check({force:true});
		// {{slug}} properties
		{{attributes}}
		cy.get('.btn-model-confirm').click();

		// assert: {{slug}} in list
		cy.url().should('include', '/backend/{{slug}}');
		cy.get('.data-list-item[data-draggable] a').filter(':contains("Cypress Test-{{ModelClass}} ' + random + '")').should('exist');
	});



/*/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */


})
